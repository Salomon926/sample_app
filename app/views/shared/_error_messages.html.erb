<% if @user.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      The form contains <%= pluralize(@user.errors.count, "error") %>
      <ul>
        <% @user.errors.full_messages.each do |msg| %>
          <% if not msg.downcase.include? 'password digest' %>
            <li> <%= msg %></li>
          <% end %>
        <% end %>
      </ul>
    </div>
  </div>
<% end %>
